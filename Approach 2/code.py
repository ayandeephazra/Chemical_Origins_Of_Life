import pysindy as ps
import numpy as np
from simulate import simulate

#############################
# increase for better results
n = 100
#############################
# 100 is realistic
# 1000 is good recovery
#############################

###############################################
days = 2
t_span = np.arange(0, days, days/n)
###############################################
sd = 0.01
###############################################

##########################################################
# enter starting condition you want to simulate
ic = np.array([0, 0.09, 0.01, 0.00, 0.00, 0])
simulate(n, t_span, sd, ic)

###################################################################

ic = np.array([0, 0.075, 0.025, 0, 0, 0])
simulate(n, t_span, sd, ic)

###################################################################

ic = np.array([0, 0.05, 0.05, 0, 0, 0])
simulate(n, t_span, sd, ic)

###################################################################

ic = np.array([0, 0.025, 0.075, 0, 0, 0])
simulate(n, t_span, sd, ic)

###################################################################

ic = np.array([0, 0.01, 0.09, 0, 0, 0])
simulate(n, t_span, sd, ic)